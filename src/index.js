import React from "react";
import ReactDOM from "react-dom/client";  // react-dom/clientをインポート
import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));  // createRootを使用
root.render(<App />);  // root.renderでAppをレンダリング
